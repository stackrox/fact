// open lots of random files under
// a specified directory
random (workers = 2, duration = 10) {
  open(random_path("/tmp"));
} : exp {
  rate = 10.0;
}

// open the same file over and over
repeated (workers = 2, duration = 10) {
  open("/tmp/test");
} : exp {
  rate = 10.0;
}
